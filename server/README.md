## INF3710 Serveur

Pour installer les modules nécessaires, veuillez exécuter la commande:
    `npm install`

Par la suite, pour démarrer le client, il suffit d'exécuter la commande:
    `npm start`

NB: Les deux commandes doivent être exécutées dans le répertoire courant (server)
